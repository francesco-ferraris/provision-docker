- name: Provision Docker
  hosts: localhost
  vars:
    gcp_project: smarket-237213 #tim-inf-col-ap00017859p65-l0
    gcp_cred_kind: serviceaccount
    gcp_cred_file: /mnt/d/Offerte di lavoro/Kiratech/service-account-key.json
    zone: "europe-west4-a"
    region: "europe-west4"
    instances:
    - centos-1
    - centos-2
    public_ip: true

  tasks:

    - name: Provision CentOS instances
      include_role:
        name: create-gcp-centos-instances
      with_items:
        - "{{ instances }}"
      loop_control:
        loop_var: instance_name

